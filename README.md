Python script that convert images pixels to gray scales, then to ASCII (@%#*+=-:. )


-> Requirements

-> Python dependencies
pip install -r requirements.txt

-> System dependencies (Linux)
sudo apt install python3-tk

